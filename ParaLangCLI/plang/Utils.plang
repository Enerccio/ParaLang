import java.util.Date;

module Utils {

    class Time {
        var date;
        
        defn init(unixTime){
            if (unixTime == NoValue)
                date = Date->Date();
            else
                date = Date->Date(unixTime);
        }
        
        defn get_time(){
            return date->getTime();
        }
    }

};