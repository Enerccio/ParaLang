using IO;
using Math;
using Math.Matrix;
using Random.Random;
using Random;
using System;
using System.BaseException;
using System.Integer;

module Main {
    var r = new Random.Random(NoValue);

    defn test(a){
        while (a) {
            try {
                return true;
            }
            finally {
                break;
            }
        }
        return false;
    }
    
    defn restricted calculate(a, b){
    
        IO.out.println((dist (4) { return run_id; }));
    
        IO.out.println(test(true));
        
        IO.out.println(new Integer(10) + new Integer(20));
    
        var ct = System.current_time();
        
        IO.out.println("Ahoj".to(3).from(1));
        
        var m1 = Math.random_matrix(a, a, r, 100);
        var m2 = Math.random_matrix(b, b, r, 50);        
        
        IO.out.println(m1);
        IO.out.println(m2);
        
        IO.out.println(m1 * 10);
        IO.out.println(m1 * m2);
        
        IO.out.println(Math.sqrt(4) + Math.sqrt(2.2));
        
        IO.out.println(System.current_time() - ct);
    }
    
};