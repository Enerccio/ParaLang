using IO;
using System;
using Math;
using Math.Matrix;
using Random;
using Collections;
using Distributed;
using Utils;

module Main {

    class XExc > BaseException {
        
    }
    
    defn restricted calculate(a, b){
        IO.out.lerp();
        
        var xxx = new [10000];
        for (var i=0; i<xxx.size(); i++){
            xxx.set(i, Random.random.get_int(1024));
        }
        
        var xxc = xxx.copy();
        
        var z = new [2];
        z.set(1, Math.is_even);
        z.set(0, xxc);
        
        IO.out.println(Utils.apply_timer(Collections.map, z));
        
    }
    
};