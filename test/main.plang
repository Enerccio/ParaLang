using IO;
using Math;
using Math.Matrix;
using Random.Random;
using Random;
using Random;
using System;
using System.BaseException;
using System.Integer;
using Collections.HashSet;
using Collections.List;

module Main {
    var r = new Random.Random(NoValue);

    defn test(a){
        while (a) {
            try {
                return true;
            }
            finally {
                break;
            }
        }
        return false;
    }
    
    defn restricted calculate(a, b){
    
        var hs = new HashSet();
        hs.insert(1);
        hs.insert(2);
        hs.insert(1);
        
        IO.out.println(hs);
        IO.out.println(hs.push(4));
        IO.out.println(hs);
        
        var ls = new List();
        ls.add(1);
        ls.add(2);
        
        IO.out.println(ls);
        IO.out.println(ls.append(3));
        IO.out.println(ls);
        IO.out.println(ls.removeAt(0));
    
        IO.out.println(test(true));
        
        IO.out.println(new Integer(10) + new Integer(20) + Random.secure_random.get_int(5));
    
        var ct = System.current_time();
        
        IO.out.println("Ahoj".to(3).from(1));
        
        var m1 = Math.random_matrix(a, a, r, 100);
        var m2 = Math.random_matrix(b, b, r, 50);        
        
        IO.out.println(m1);
        IO.out.println(m2);
        
        IO.out.println(m1 * 10);
        IO.out.println(m1 * m2);
        
        IO.out.println(Math.sqrt(4) + Math.sqrt(2.2));
        
        IO.out.println(System.current_time() - ct);
    }
    
};