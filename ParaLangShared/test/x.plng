using IO;
using Collections.List;

module HelloWorldModule {

	class Test {
		var a, b;
		
		restricted __apply(){
			run();
		}
		
		restricted run(){
			b = 6;
			a = new Integer(b);
			IO.out.println(a == b);
			IO.out.println(b == a);
			
			var c = 5;
			var d = 8;
			
			IO.out.println(c > new Integer(a));
			IO.out.println(d < a);
			
			IO.out.println(c + a + b);
			
			inst == new Test();
		}
		
		__eq(me, other){
			IO.out.println("Test!");
			return parent.__eq(me, other);
		}
	}
	
	restricted run(){
		new Test()();
	}
};