using IO;
using Collections.List;

module HelloWorldModule {
	
	restricted run(){
		for (var x=0; x<1000000; x++){
			if (x == 101) break;
			if (x % 10 != 0) continue;
			
			IO.out.println(x);
		}
	}
};